[project]
name = "job-search-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12.0"
dependencies = [
    "anthropic>=0.94.0",
    "mistune>=3.2.0",
    "notion-client>=3.0.0",
    "pytest>=9.0.3",
]
